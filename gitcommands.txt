eigenen branch auf neuesten stand bringen:
git commit -m "info zum commit" 
//alle changes commiten, keine änderungen local lassen. entweder commit, stash, oder changes reverten

git push (am besten mit github desktop oder git kraken weil idk wie man github und git bash verbindet)

git checkout dev

git pull origin *name deines branches*

git push (am besten mit github desktop oder git kraken weil idk wie man github und git bash verbindet)

git checkout *name deines branches*

git pull origin dev

git push (am besten mit github desktop oder git kraken weil idk wie man github und git bash verbindet)
